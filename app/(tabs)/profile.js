import { StyleSheet, View } from "react-native";

export default function Profile() {
  return <View style={styles.container} />;
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#000", // you can make this white or gradient later
  },
});
